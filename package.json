{
  "name": "terraform-monorepo-example",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:backend": "pnpm --filter backend start",
    "start:frontend": "pnpm --filter frontend start",
    "start": "concurrently \"pnpm start:backend\" \"pnpm start:frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
